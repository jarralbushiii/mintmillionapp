<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MintMillionApp">
        <activity android:name=".splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".wallet.WalletActivity" />
        <activity android:name=".games.AviatorGameActivity" />
        <activity android:name=".games.RabbitRunnerGameActivity" />
        <activity android:name=".games.RollDiceGameActivity" />
        <activity android:name=".games.LudoGameActivity" />
        <activity android:name=".games.RouletteGameActivity" />
        <activity android:name=".games.GamesActivity" /> <!-- Added -->
        <activity android:name=".nft.NFTMarketplaceActivity" />
    </application>
</manifest>